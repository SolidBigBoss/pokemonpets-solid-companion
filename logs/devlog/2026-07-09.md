# Devlog — 2026-07-09

## Teste real (validou a pendência de 08/07)
- Boss confirmou: a lógica de fase `attack`/`capture` baseada no HP do inimigo está funcionando corretamente em jogo real. Fecha a pendência principal deixada em 08/07.
- Ainda restam alguns cenários específicos não tratados, identificados no teste — Boss vai detalhar quais na sequência (aguardando).

## Integração com Git/GitHub via Windows-MCP (novo)
- Ficou disponível o acesso `Windows-MCP` (PowerShell, FileSystem, Process, etc.) — permite rodar comandos diretamente na máquina real do Boss, fora do sandbox Linux.
- Confirmado que já está tudo configurado do lado do Windows: repositório `C:\pokemonpets-bot` limpo, branch `main` sincronizada, identidade git (`SolidBigBoss` / `gustavo-amaral@hotmail.com.br`), e `gh` autenticado com escopo `repo` (permite push).
- Combinado o fluxo: quando o Boss pedir pra commitar, faço `git add` + `git commit` com mensagem descritiva, **sem push automático** — push só acontece se pedido explicitamente ("commita e sobe"/"commita e dá push").
- Consultado o histórico de commits via `git log` (equivalente ao que aparece no Source Control/Git Graph do VS Code): 4 commits até agora, todos de `SolidBigBoss` (`Initial commit`, `update 0.1`, `update 0.11`, `update 0.11 reorganized`).

## Revisão do fluxo completo
- A pedido do Boss, descrevi o fluxo ponta a ponta (inicialização, loop principal, os 7 estados e handlers, lógica de combate por fase de HP, scrape da espécie, persistência) pra alinhar entendimento antes de fazer ajustes.
- Reforçada a intenção combinada: deixar o projeto "100% redondo" (estabilizar o que já existe) antes de partir pra novas implementações do backlog.

## Lembrete de processo (pedido explícito do Boss)
- Verificar a data atual no início de cada sessão e manter o devlog do dia atualizado conforme informações relevantes forem surgindo — não só no fechamento da sessão.

## Cenário sem move efetivo + pedido de dados detalhados do time
- Boss reportou um cenário real de combate: o pokémon em luta não tinha nenhum move efetivo (todos causariam 0 de dano) — precisa de lógica pra trocar de pokémon nesse caso ou quando os PPs acabarem.
- Boss quer persistir dados DETALHADOS de cada pokémon (natureza, IVs, EVs, habilidades, moveset+PP) — diferente da tabela `captured_pokemon` atual, que só tem dados genéricos de espécie.
- Ordem combinada: primeiro time (`ManageTeam.aspx` → `SeeUserPokemonDetails.aspx?ID=X`), depois capturados, depois dados genéricos (link "poke_resim").

## Bugs corrigidos (3, a pedido do Boss)
- Seleção de Poké Ball não aplicava de fato (dropdown abria mas não selecionava) — corrigido em `actions.py`: removido atalho de "já selecionado", trocado clique JS sintético por clique real do Playwright, com verificação pós-clique. **Confirmado funcionando pelo Boss em jogo real.**
- Restart do bot no meio de uma batalha não detectava a batalha em andamento (achava que precisava logar de novo). Corrigido em `bot.py`/`main()`: condição de "precisa logar" agora é "fora do domínio pokemonpets OU na tela de login/welcome", em vez de exigir estar especificamente em `gamepage.aspx`.
- Mensagem de falha do jogo "Please select a Poké Ball first..." não tratada — já existia leitura do `FailMessage` em `state.py` (de 08/07), reconfirmado nesse fluxo.

## Nova tabela `team_pokemon` + scraper `team.py` (fonte 2)
- Criada tabela `team_pokemon` em `db.py` com todos os campos detalhados (nature, IVs, EVs, stats, moveset+PP, habilidades, etc.), chave primária `identity_no`, upsert via `save_team_pokemon()`.
- Criado `team.py`: `get_team_identity_ids()` (lê os 6 IDs do time em `ManageTeam.aspx`), `scrape_pokemon_details()` (abre `SeeUserPokemonDetails.aspx?ID=X` numa aba nova e extrai todos os campos por matching de rótulo normalizado — sem acento/pontuação), `sync_team()` (orquestra tudo e salva no banco).
- Lógica de scraping validada localmente com jsdom (Node) contra o HTML real que o Boss mandou (Lapras), antes de qualquer teste em jogo — pegou 2 bugs reais (matching de rótulo com prefixo "Pokémon's ", e leitura errada do campo `gender` que é um `<select>`). Ainda não testado contra o jogo ao vivo.
- **Bug de infraestrutura encontrado e contornado**: escrever `team.py` via as ferramentas normais de arquivo (Write/Edit) estava truncando o arquivo silenciosamente (sobra de conteúdo de escritas anteriores maiores, preenchida com espaços/nulls). Contornado escrevendo o arquivo via `heredoc` no bash diretamente no mount — funcionou e o arquivo ficou íntegro (validado com `ast.parse` e `node --check` nos blocos JS).

## Próximos passos combinados (HUD)
- Boss sugeriu criar uma HUD (GUI) com botões de ação manual: sincronizar time, iniciar captura, parar.
- Decisão do Boss: construir a casca da HUD primeiro (todos os 3 botões já no lugar) antes de aprofundar a lógica de scraping/captura.
- Botão "Iniciar Captura" já nasce com seletor de modo, mas por enquanto só a opção "Capturar tudo" (única lógica real que existe hoje).
- Faltam: subscriber de log em `logger.py` (pra HUD mostrar logs em tempo real), `stop_event` em `main()` (pra HUD conseguir parar o loop), e o `hud.py` em si (tkinter).

## Pendências para a próxima sessão
- Testar `team.py`/`sync_team()` contra o jogo real (ainda só validado via jsdom).
- Seguem pendentes: raridade real (`stars`) fixa em 1 no `choose_pokeball`; scraping detalhado de pokémons capturados e dados genéricos (poke_resim) — depois do time; cenário de "nenhum move efetivo"/troca de pokémon — depende dos dados do time.
