# Devlog — 2026-07-10

## Continuação da HUD (Fase 4)
- `logger.py`: adicionado mecanismo de subscriber (`subscribe(fn)`). `log()` agora chama todos os assinantes registrados, além do `print()` normal. Módulo continua sem saber nada de tkinter/threading — quem se inscrever cuida do marshaling entre threads (a HUD vai usar uma `queue.Queue()`).
- `bot.py`: `main()` agora aceita `stop_event: threading.Event = None` opcional. A cada iteração do loop principal, se `stop_event` estiver setado, sai do loop com o mesmo log de resumo do Ctrl+C. Rodando direto (`python bot.py`), comportamento não muda (parâmetro fica `None`).
- Falta só: criar `hud.py` (janela tkinter com os 3 botões — Sincronizar Time, Iniciar Captura com seletor de modo, Parar).

## Discussão de schema — unificar tabelas de pokémon
- Boss propôs unificar `team_pokemon`/dados detalhados numa única tabela `captured_pokemon`, com campo `in_team BOOLEAN` e timestamp de atualização, em vez de duas tabelas desconectadas.
- Concordamos que faz sentido, com uma ressalva: `captured_pokemon` hoje é um log de eventos de captura (sem `identity_no`, só dados genéricos da espécie), enquanto `team_pokemon` é um registro por indivíduo real (chave `identity_no`). Unificar de verdade exige capturar o `identity_no` do pokémon logo após a captura — isso ainda não existe.
- Confirmado: hoje não existe nenhum campo `in_team` explícito — "estar no time" é implícito (a linha existe em `team_pokemon` porque veio de `ManageTeam.aspx`). `sync_team()` também nunca marca "saiu do time" — só faz upsert, nunca reconciliação. Isso vai precisar ser tratado na sincronização quando o campo `in_team` existir (marcar `false` em quem não apareceu na leitura atual, antes de marcar `true` nos 6 lidos).
- Migração de schema fica pra depois da HUD, como combinado.

## Lembrete de processo
- Confirmada a checagem de data no início da sessão, conforme pedido do Boss (hoje é um novo dia, 10/07, sessão anterior estava registrando em 09/07).

## Pendências para a próxima sessão
- Criar `hud.py`.
- Revisar o schema (unificação de `captured_pokemon`/`team_pokemon` com `in_team` + reconciliação no sync).
- Testar `team.py`/`sync_team()` contra o jogo real (ainda só validado via jsdom).
